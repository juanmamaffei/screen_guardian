<%= form_with(model: screen_time) do |form| %>
  <% if screen_time.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(screen_time.errors.count, "error") %> prohibited this screen_time from being saved:</h2>

      <ul>
        <% screen_time.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :duration, style: "display: block" %>
    <%= form.number_field :duration %>
  </div>

  <div>
    <%= form.label :screen_type, style: "display: block" %>
    <%= form.text_field :screen_type %>
  </div>

  <div>
    <%= form.label :app_name, style: "display: block" %>
    <%= form.text_field :app_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
